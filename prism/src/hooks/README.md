# hooks/

Reusable React hooks: camera control, hover/selection state, animated
number counters, media queries, GSAP/Framer Motion helpers.

Populated starting Phase 4 (interaction hooks) and Phase 5 (camera hooks).
