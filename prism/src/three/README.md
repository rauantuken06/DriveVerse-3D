# three/

The R3F/Three.js scene: Canvas setup, PrismCore, DepartmentNode,
connection lines, particle field, camera rig, risk-propagation and
simulation visual effects. Reads scene-relevant state from `store/` —
never owns UI state itself.

Populated starting Phase 3.
