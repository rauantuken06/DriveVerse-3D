# components/

Top-level screen/layout React components (Landing, AppShell, CEO Mode
overlay, Simulation panel, AI Command Bar, Department View). These
compose primitives from `ui/` and the 3D scene from `three/`.

Populated starting Phase 2.
